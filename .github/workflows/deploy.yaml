name: Build Python package and upload artifact

on:
  push:
    branches: [feature/api_image]
  pull_request:

jobs:
  build-api:
    uses: ./.github/workflows/build-wheel.yaml
    with:
      package-path: api
      wheel-name: api-wheel
